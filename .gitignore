node_modules/
dist/
uploads/

# SQLite database and WAL mode sidecars
*.db
*.db-shm
*.db-wal
server/data/

# Environment
.env
client/.env.local

# Build output served by Express during production
server/public/

# Claude Code local permission grants and MCP config (machine-specific, contains secrets)
.claude/settings.local.json
.claude/settings.json

# Personal data — not committed
scripts/watchlist.json

# TypeScript compiler output in source directories (tsc without outDir)
client/src/**/*.js
client/src/**/*.js.map

# Runtime artifact and notebook files (user data, not code)
artifacts/
notebooks/

# Test artifacts
test-results/
playwright-report/
coverage/
screenshots/

# macOS
.DS_Store

# Local token optimization — keep .cursorignore out of repo
.cursorignore
